# This build uses a hardcoded reference dataset for S&P 500 returns (see
# src/data/sp500Fallback.ts) — no live API key is required to run the app.
#
# Reserved for a future live-data integration (Section 3.3, Option A): if
# Alpha Vantage is wired up later, set this to a free-tier API key from
# https://www.alphavantage.co/support/#api-key
VITE_ALPHA_VANTAGE_KEY=
